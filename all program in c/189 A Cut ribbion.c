#include <stdio.h>

int main()
{
    int n,a,b,c,s[3];
    scanf("%d%d%d%d",&n,&a,&b,&c);
    for(i=0;i<3;i++)
    {
        s[i]=n/a;
        if(((n%a)%b)==0 || ((n%a)%c)==0)
        {
            s=s
        }
    }
}
