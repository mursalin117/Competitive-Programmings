#include <stdio.h>
#include <string.h>

int palindrome(int x);

int main()
{
    long int n;
    int t,i,s;
    scanf("%ld",&t);
    for(i=1;i<=n;i++)
    {
        scanf("%ld",&n);
        s=palindrome(n);
    }
}

int palindrome(int x)
{
    a
}
