#include <stdio.h>
int main()
{
    int a,b,c,d,f,s;
    scanf("%d%d",&a,&s);
    int ar[a],d[s];
    for(b=0;b<a;b++)
    {
        scanf("%d",&ar[b]);
    }
        for(c=(a-1);c>=0;c--)
     printf("%d",ar[c]);


                d[a]=ar[c];


     }
