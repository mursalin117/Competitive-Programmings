#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n, i;
    string s;

    scanf("%d", &n);
    getchar();
    scanf("%s", s);
    for(i = 0; i < n; i++)
    {
        if(s[i] <= s[i+1]) continue;
        else
    }
}
