#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n,i,j,k,l,coun = 0,x;
    vector < int > v;

    cin >> n >> k;
    for(i = 0; i < n; i++)
    {
        cin >> x;
        v.push_back(x);
    }
    for(i = 1; ; i++)
    {
        for(j = 0; j < n; j++)
        {
            for(l = 1; l <= v[j]; l++)
            {

            }
        }
    }
}
