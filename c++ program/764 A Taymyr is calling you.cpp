#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n, m, z, d;
    scanf("%d%d%d", &n, &m, &z);
    if(n > m)
    printf("%d\n", d);
    return 0;
}
