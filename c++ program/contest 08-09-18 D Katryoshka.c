#include <stdio.h>
#include <stdlib.h>

int main()
{
    long int n, a, b, c, s, i, min;

    freopen("katryoshka.in", "r", stdin);

    scanf("%d", &n);
    for(i= 0; i < n; i++)
    {
        scanf("%d%d%d",&a,&b,&c);
        min = a;
        if()
    }
}
