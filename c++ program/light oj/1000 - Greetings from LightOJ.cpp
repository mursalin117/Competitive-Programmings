#include <bits/stdc++.h>

using namespace std;

int main(){
    int t, i, n, m;
    scanf("%d", &t);
    for(i = 0; i < t; i++){
        scanf("%d%d", &n, &m);
        printf("Case %d: %d\n", i+1, n + m);
    }
    return 0;
}
